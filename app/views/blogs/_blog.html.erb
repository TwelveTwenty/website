<article class="full">
	<%= image_tag blog.artwork, :class => "artwork" %>

	<div class="content">
		<div class="padded">
			<header>
				<% if defined? detail %>
					<h1><%= blog.title %></h1>
				<% else %>
					<%= link_to blog do %>
						<h1><%= blog.title %></h1>
					<% end %>
				<% end %>
				<h2>Written by <%= blog.author %></h2>
			</header>
			<%= simple_format blog.body %>
		</div>
	</div>
	<footer></footer>

</article>