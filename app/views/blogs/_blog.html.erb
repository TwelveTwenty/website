<article class="full">
	<%= image_tag blog.artwork_url(:full).to_s, :class => "artwork" %>

	<div class="content">
		<div class="padded">
			<header>
				<% if defined? detail %>
					<h1><%= blog.title %></h1>
				<% else %>
					<%= link_to blog do %>
						<h1><%= blog.title %></h1>
					<% end %>
				<% end %>
				<h2>Written by <%= blog.author %></h2>
			</header>
			<%= markdown blog.body %>
			<%  if current_user %>
				<ul>
					<li><%= link_to "Edit this blog post", edit_blog_path(blog), {:class => 'arrow'} %></li>
					<li><%= link_to "Destroy this blog post", blog, method: :delete, :class => 'arrow', data: {confirm: 'Are you sure?'} %></li>
				</ul>
			<% end %>
		</div>
	</div>
	<footer></footer>

</article>